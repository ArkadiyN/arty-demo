{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'if echo \"$CLAUDE_TOOL_INPUT\" | grep -qE \"git (commit|push)\"; then CURRENT_BRANCH=$(git branch --show-current 2>/dev/null || git rev-parse --abbrev-ref HEAD 2>/dev/null); if [[ \"$CURRENT_BRANCH\" == \"main\" || \"$CURRENT_BRANCH\" == \"master\" ]]; then echo \"BLOCKED: Direct commit/push is prohibited on branch: $CURRENT_BRANCH\"; exit 1; fi; fi'"
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
     "WebFetch",
     "WebSearch",
     "Write(.claude/worktrees/*)",
     "Edit(.claude/worktrees/*)",
     "Bash(uv run *)",
     "Bash(uv *)",
     "Bash(quarto *)",
     "Edit(src/arty/**)",
     "Write(src/arty/**)",
     "Edit(experiment/**)",
     "Write(experiment/**)",
     "Edit(doc-reference/**)",
     "Write(doc-reference/**)",
     "Edit(tests/**)",
     "Write(tests/**)"
    ]
  },
  "worktree": {
    "baseRef": "head"
  }
}
