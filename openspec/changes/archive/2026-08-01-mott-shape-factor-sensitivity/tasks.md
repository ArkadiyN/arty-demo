## 1. src/arty physics change (@modeler, per agents-routing.md Gate 2)

- [x] 1.1 Add `aspect_ratio: float = 1.6` and `breadth_factor: float = 1.5` fields to `ShellParams` in `src/arty/fragmentation.py`, with a comment tying them to the Mott fragment-shape closure (distinct from ogive/shell geometry fields) and citing `experiment/fragmentation-field/updates/mott-fragment-shape-closure/derivation.md`.
- [x] 1.2 Update `mott_params(shell, V0)` to read `shell.aspect_ratio` / `shell.breadth_factor` instead of the module-level `_MOTT_ASPECT_RATIO` / `_MOTT_BREADTH_FACTOR` constants; keep those constants as the dataclass field defaults (single source of truth) rather than deleting them outright if other code still references them.
- [x] 1.3 Add/extend `tests/test_fragmentation.py` coverage: (a) default-preserving — `mott_params` output with unset `aspect_ratio`/`breadth_factor` matches the existing reviewed baseline within tolerance (spec scenario "mott_params default shape factors preserve existing output"); (b) override behavior — raising `aspect_ratio` above 1.6 increases `mu` relative to the default call, all else fixed (spec scenario "mott_params responds to an overridden aspect ratio").
- [x] 1.4 Run `uv run pytest tests/test_fragmentation.py -v` and the full suite (`uv run pytest -q --ignore=tests/test_pdf_processor.py` — the PDF-processor tests hit a live external API and are excluded from routine runs) and confirm no regressions.

## 2. Sensitivity app wiring (main agent — UI/layout only, no physics)

- [x] 2.1 In `app/sensitivity.py`, add two sliders to the existing "Mott Fragmentation" group: aspect ratio `A` (1.50–1.71, default 1.60) and breadth factor `κ_x` (1.00–2.00, default 1.50) — per spec scenario "Mott group exposes both fragment-shape factors".
- [x] 2.2 Wire both slider values through to every `ShellParams(...)` construction site in `app/sensitivity.py` (mirroring how `gamma`/`sigma_f` are already threaded through), setting `aspect_ratio=` / `breadth_factor=`.
- [x] 2.3 Confirm the existing `@st.cache_data` keys used for the cached 3D-field computation include the two new slider values, so moving either slider triggers a recompute (per the existing "Expensive recomputation is cached" requirement) — per spec scenario "Fragment-shape sliders change fragment mass output live".
- [x] 2.4 Manually launch the app (`uv run streamlit run app/sensitivity.py`), move the `A` slider off its default, and confirm the Mott cumulative distribution chart and `R₅₀` metric change; move it back to default and confirm the chart matches the pre-change baseline. **Note:** no browser tool was available in this environment; verified functionally instead by calling the app's exact compute path (`compute_frag_field_3d` for Single-zone mode, `compute_shell_zones` for Four-zone mode) with default vs. `A=1.71` `ShellParams`, confirming `mu`/`N0` shift in both modes and round-trip back to the exact baseline on return to defaults. Also confirmed the app boots cleanly (HTTP 200, no traceback in logs) with the updated `src/arty` code. This verification also surfaced and led to fixing a real gap: the Four-zone path's per-zone Mott closure wasn't reading the shape-factor fields at all prior to a follow-up fix in `src/arty/zones.py` (reviewed PASS-with-limitations, see `updates/mott-fragment-shape-closure/review.md` addendum).

## 3. Notebook/documentation touch-up

- [x] 3.1 If `experiment/fragmentation-field/_parameters.qmd` (or wherever `ShellParams` fields are documented for readers) lists shell parameters, add the two new fields there, importing from `src/arty` — no physics values duplicated into the `.qmd`.
